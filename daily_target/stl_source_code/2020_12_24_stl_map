map的特性是所有元素都会根据元素的键值自动排序, map的所有元素都是pair,同时拥有实值(value)和键值(key) pair的第一元素被视为键值,第二元素被视为实值.map不允许两个元素拥有相同的键值

关于map的迭代器
我们可以通过迭代器改变迭代器的实值 但是不能改变键 主要原因同set所以set的迭代器是const iterator, 因为改了就会严重破坏map组织

在增删操作之前的迭代器在增删操作过后依然有效 除了删除的那个元素的迭代器之外 因为他已经断开了连接他所有的指向都指向了NULL

RB_Tree是一种平衡二叉树自动排序效果很不错 因为插入并且调整平衡以后就已经有序了
